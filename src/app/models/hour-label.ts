/** Defines the coordinates for the calendar hour labels */
export interface HourLabel {
  x: string; // x coordinate of the starting point of the text baseline
  y: string; // y coordinate of the starting point of the text baseline
}
